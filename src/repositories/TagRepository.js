const { Tag } = require('../models');

class TagRepository {
    async create(data) {
    }

    async findAll() {
    }

    async findById(id) {
    }

    async deleteById(id) {
    }
}

module.exports = new TagRepository();
